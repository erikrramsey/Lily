Graphics:
	Material System:
		Make simplest one possible, expand
	Importer:
		Fix the importer to allow for different texture filepaths
		Probably just wrap assimp entirely at some point
x   Add a transform argument to the draw function
	

Editor:
x   Create an editor lol
	Clean up LilyApplication so that it inerits from an application class
x   Handle input in application figure out some way to send signals to the engine ( remove or redo event manager ?)
x	Maybe add some gui?

EC_Registry: (Number one priority)
	Completely fix / redo
	Create some test cases / learn how to test properly


IDEAS:
	Lua scripting